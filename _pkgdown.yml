url: https://nicebread.github.io/OAmetrics/
template:
  bootstrap: 5

build:
  vignettes: "lazy"  # Ensures only changed vignettes are rebuilt

repo:
  url:
    source: https://github.com/nicebread/OAmetrics

navbar:
  structure:
    right: [github]
